{
  "name": "quorum",
  "version": "0.0.0",
  "private": true,
  "description": "Slack for coding agents \u2014 rooms, events, and overlap-checked claims over MCP",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p src/ui",
    "dev:tls": "sh scripts/dev-tls.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3"
  }
}
